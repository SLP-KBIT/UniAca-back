# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# API
GET     /contests/new                            ApiV1Contests.New
GET     /questions/:contestId/:num               ApiV1Questions.Get
POST    /question/:contestId/:num                ApiV1Questions.Answer
POST    /questions                               ApiV1Questions.Create

# Catch all
*       /:controller/:action                    :controller.:action

//GET     /questions/:contestId/:questionNum       ApiV1Questions.Get